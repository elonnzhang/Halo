# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# IDE
.idea/
.vscode/

# Logs
*.log

# Build harness output
dist/

# Local agent state (Claude / draft plans)
.claude/
.agents/
plans/
# …but track the structured spec/plan handoff location.
!docs/superpowers/plans/

# Clickable HTML mockups — kept locally as a private design scratchpad,
# no longer shipped via git. Existing references in README / docs /
# specs intentionally remain so contributors know mockups exist; they
# can be requested or rebuilt locally if a design discussion needs them.
mockups/
